{
  "name": "cita-ciudadana",
  "version": "1.0.0",
  "description": "App Cita Ciudadana preparada para Capacitor y Firebase",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "cap:init": "npx cap init",
    "cap:sync": "npx cap sync",
    "cap:build:android": "npx cap copy android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
